# 2026-06-26 工作日志

## 完成事项
- 从 Claude 交接文档继续：实现了 fetch_real_data() 真实数据获取
- 数据源：腾讯财经（指数）+ 东财push2（行业）+ 同花顺热点（题材）+ 东财快讯（催化）+ 东财龙虎榜
- 用真实数据模式生成了今日报告 a_stock_daily_summary_2026-06-26.html
- 创建 index.html 作为 GitHub Pages 入口
- 初始化 Git 仓库、配置身份、提交代码
- 创建定时任务：每周一到五 16:05 运行 --real 模式
- GitHub 推送需用户手动完成（需要认证/PAT）

## 待用户完成
- 在 GitHub 上创建 a-stock-report 仓库
- 配置 PAT 推送代码
- 开启 GitHub Pages

## 02:06 自动化任务执行
- automation-1782410690342 周五 16:05 准时触发
- `generate_report.py --real` 成功运行，30,715 字节
- 5/6 数据源正常：指数/强势股/快讯/龙虎榜 ✅，push2 行业板块仍被沙盒代理拦
- index.html 已同步最新报告
- 经验：沙盒环境的 push2.eastmoney.com 拦截是稳定现象，不必每次都试

## 02:07 自动化任务再次执行（手动触发复测）
- 复跑 `generate_report.py --real` 仍然 5/6 数据源成功，板块源持续被沙盒反代拦截
- 输出 30,715 字节报告（与 02:06 第一次输出一致）
- index.html 已同步（cp 命令直接覆盖）
- 复现稳定，无新错误

## 02:30 行业板块数据源修复（用户要求处理）
- **用户反馈**：push2 行业板块在沙盒里 ProxyError，代理是 Clash Verge 规则模式
- **诊断过程**：
  1. `printenv | grep -i proxy` 发现沙盒残留 `127.0.0.1:7897`（Clash 端口）
  2. 脚本入口加 `os.environ.pop` 清除，但 `requests` 仍 RST
  3. 改 `env -u` 在子进程层清除，仍然 RST
  4. curl 直接测也是 `exit 52`（0.1s 服务端 RST）
  5. 改用 `subprocess.check_output(["curl", ...])` 调用 curl，**仍失败**（Python TLS 不通）
  6. 改用 `http://` 协议（之前 curl 测试这个通）→ 502 服务器拒
  7. 扫描 `push2.*.eastmoney.com` 所有子域名 → **`push2delay.eastmoney.com` 返回 200！**
  8. 验证 push2delay 返回完整 10 行行业数据（航空机场 +3.93% 领涨）
- **最终方案**：fetch_real_data() 行业板块请求按 `("push2delay", "push2")` 顺序尝试，第一个成功就 break
- **结果**：6/6 数据源全通，30,838 字节报告，真实行业数据落地
- **教训**：
  - 沙盒里 Python requests 不通但 curl 通的怪异现象，原因未明（可能是 Python TLS 指纹被风控）
  - 子域名扫描是解决"被风控"问题的银弹，东财至少有 push2/push2delay 两个镜像
  - 备用源降级链要尽量长：镜像1 → 镜像2 → akshare → mock

## 07:33 日期选择器同步修复
- **用户反馈**：历史报告跳转后无法再选择别的日期（旧模板没有 dateSelect）
- **修复方案**：`update_all_date_selectors()` 处理两种情况：
  1. 已有 dateSelect → 替换 `<select>` 选项，selected 指向该文件自身日期
  2. 没有 dateSelect 的旧模板 → 注入完整日期导航栏 + goToDate 脚本
- `_build_select_html()` 辅助函数按文件自身日期设置 selected
- 6月26日旧模板成功升级（注入了完整 date-nav + script）
- 已推送 GitHub

## 08:36 日期标签修正
- **用户反馈**：6-26和6-27的日期标签与实际数据不匹配
- **修复**：批量替换文件名+内部日期引用
  - `a_stock_daily_summary_2026-06-26.html` → `2026-06-25.html`（内部日期全部改为6-25）
  - `a_stock_daily_summary_2026-06-27.html` → `2026-06-26.html`（内部日期全部改为6-26）
- 日期选择器选项同步更新（6-25页面可选跳6-26，6-26页面可选跳6-25）
- index.html 更新为最新的6-26报告
- 已推送 GitHub
